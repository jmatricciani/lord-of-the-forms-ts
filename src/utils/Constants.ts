export const firstNameErrorMessage =
  "First name must be at least 2 characters long";
export const lastNameErrorMessage =
  "Last name must be at least 2 characters long";
export const emailErrorMessage = "Email is Invalid";
export const cityErrorMessage = "State is Invalid";
export const phoneNumberErrorMessage = "Invalid Phone Number";

export const VALID_EMAIL_REGEX =
  // eslint-disable-next-line no-useless-escape
  /^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$/;
export const NUMERICAL_REGEX = /[^0-9]/g;
export const MIN_NAME_LENGTH = 2;
export const PHONE_INPUT_LENGTH = 7;
